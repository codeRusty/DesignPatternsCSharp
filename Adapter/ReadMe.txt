Intent to use:

When we require to convert an interface of a class to another interface which client expects (mostly when replacing a old legacy system to a new one.)

When to use:

To Wrap an Existing class with a new interface.
To perform impedance matching.